# physics.cmake — intentionally empty.
#
# Physics application code (SPH fluid, heat, EM field) is decoupled from the
# numerics library and lives in the separate numerics-apps repository.
# The numerics library (target: numerics) is purely numerical analysis.
